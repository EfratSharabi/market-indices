import { IndiceChangePipe } from './indice-change.pipe';

describe('IndiceChangePipe', () => {
  it('create an instance', () => {
    const pipe = new IndiceChangePipe();
    expect(pipe).toBeTruthy();
  });
});
